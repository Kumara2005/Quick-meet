# QuickMeet — Environment Configuration

# HTTP/WebSocket server port (default: 3000)
PORT=3000

# Node environment: development | production
NODE_ENV=development

# Server log level: debug | info | warn | error | silent
# Production default is warn if unset
LOG_LEVEL=debug
